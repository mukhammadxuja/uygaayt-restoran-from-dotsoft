// Development mode configuration
// Set to true to bypass authentication (login will be skipped)
// Set to false to enable authentication (normal production mode)
export const DEV_MODE_BYPASS_AUTH = true;

